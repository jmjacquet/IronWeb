version: '3.5'
services:        
  django:            
    build: .
    tty: true        # docker run -t
    container_name: django-ironweb       
    volumes:
     - .:/ironweb
    ports:
     - "8000:8000"
    
